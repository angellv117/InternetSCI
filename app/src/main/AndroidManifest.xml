<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.interntsci">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InterntSCI">
        <activity
            android:name=".leerQR"
            android:exported="true" />
        <activity
            android:name=".crearQR"
            android:exported="true" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".EditEliCliuente" />
        <activity android:name=".AgregarCliente" />
        <activity android:name=".Clientes" />
        <activity android:name=".EliminarEditar" />
        <activity android:name=".AgregarNota" />
        <activity android:name=".pendientes" />
        <activity android:name=".menu" />
        <activity android:name=".recuperar" />
        <activity android:name=".registro" />
        <activity android:name=".alta" />
        <activity android:name=".opciones" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>